import { VoirPlusDirective } from './voir-plus.directive';

describe('VoirPlusDirective', () => {
  it('should create an instance', () => {
    const directive = new VoirPlusDirective();
    expect(directive).toBeTruthy();
  });
});
